<snippet>
	<content><![CDATA[
void DJ(ll tc)
{
	ll n, sum = 0;
	cin >> n;
	vector<ll> a(1, 0);
	for (ll i = 1; i <= n; i++)
	{
		ll x;
		cin >> x;
		sum += x;
		a.pb(x);
	}
	cout << sum << "\n";
}
]]></content>
	<!-- Optional: Set a tabTrigger to define how to trigger the snippet -->
	<tabTrigger>trigeritout</tabTrigger>
	<!-- Optional: Set a scope to limit where the snippet will trigger -->
	<scope>source.c++</scope>
</snippet>s
