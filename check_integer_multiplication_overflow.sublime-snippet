<snippet>
	<content><![CDATA[
bool check_integer_multiplication_overflow(int a, int b)
{
    int c = a * b;
    if (a == 0 || b == 0)
        return false;
    if (c / a != b)
        return true;
    return false;
}
]]></content>
	<!-- Optional: Set a tabTrigger to define how to trigger the snippet -->
	<tabTrigger>tcheck_integer_multiplication_overflow</tabTrigger>
	<!-- Optional: Set a scope to limit where the snippet will trigger -->
	<scope>source.c++</scope>
</snippet>
